This repo contains the Visual Studio 2015 solution found at http://fabrikam.codeplex.com/SourceControl/latest

The only change for first checkin was to update the web app's web.config connection strings to point at an Azure SQL DB instance.

This repo is for people to clone or fork for testing, learning, etc.